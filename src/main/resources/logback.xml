<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/electroland.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/electroland.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Google Cloud Logging Appender -->
    <appender name="CLOUD" class="com.google.cloud.logging.logback.LoggingAppender">
        <projectId>electroland-454016</projectId>  <!-- Ensure the correct projectId is specified -->
        <logName>application-log</logName>         <!-- This is the name of the log in Google Cloud -->
        <jsonPayload>true</jsonPayload>           <!-- This ensures logs are in JSON format -->
    </appender>
    
    <!-- Log HTTP requests -->
    <logger name="org.springframework.web" level="DEBUG" />
    
    <!-- Log SQL queries -->
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />
    
    <!-- Log application activities -->
    <logger name="fpoly.electroland" level="DEBUG" />
    
    <!-- Log security activities -->
    <logger name="org.springframework.security" level="DEBUG" />
    
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="CLOUD" />
    </root>
</configuration>
